# Local development (SQLite — default)
DATABASE_URL="file:./dev.db"
PORT=4000
FRONTEND_URL=https://reading-hub-web.onrender.com
UPLOADS_DIR=./uploads

# Production on Render (PostgreSQL) — set in Render dashboard:
# DATABASE_URL=<from Render Postgres internal URL>
